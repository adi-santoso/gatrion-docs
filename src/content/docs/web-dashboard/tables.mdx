---
title: "Tables"
project: "Web Dashboard"
category: "Components"
description: "Komponen Table dengan sorting, pagination, dan row selection. Built dengan TanStack Table."
---

import Callout from '../../../components/Callout.astro';
import CodeBlock from '../../../components/CodeBlock.astro';
import NextSteps from '../../../components/NextSteps.astro';
import NextStepCard from '../../../components/NextStepCard.astro';

<section class="animate-fade-up [animation-delay:.1s]">
<h2 id="basic-table" class="group scroll-mt-24 text-xl font-bold tracking-tight text-zinc-900 dark:text-white">
  <a href="#basic-table" class="flex items-center gap-2">Basic Table <span class="text-violet-400 opacity-0 transition group-hover:opacity-100">#</span></a>
</h2>

<div class="mt-4 overflow-hidden rounded-xl border border-zinc-200 dark:border-white/10">
  <table class="w-full text-sm">
    <thead class="border-b border-zinc-200 bg-zinc-50 dark:border-white/10 dark:bg-white/5">
      <tr>
        <th class="px-4 py-3 text-left font-semibold text-zinc-700 dark:text-zinc-300">Name</th>
        <th class="px-4 py-3 text-left font-semibold text-zinc-700 dark:text-zinc-300">Email</th>
        <th class="px-4 py-3 text-left font-semibold text-zinc-700 dark:text-zinc-300">Role</th>
        <th class="px-4 py-3 text-left font-semibold text-zinc-700 dark:text-zinc-300">Status</th>
      </tr>
    </thead>
    <tbody class="divide-y divide-zinc-100 dark:divide-white/5">
      <tr class="hover:bg-zinc-50 dark:hover:bg-white/[.02]">
        <td class="px-4 py-3 text-zinc-900 dark:text-white">John Doe</td>
        <td class="px-4 py-3 text-zinc-500">john@gatrion.com</td>
        <td class="px-4 py-3"><span class="rounded-md bg-violet-500/10 px-2 py-0.5 text-xs font-medium text-violet-600 dark:text-violet-300">ADMIN</span></td>
        <td class="px-4 py-3"><span class="inline-flex h-2 w-2 rounded-full bg-emerald-500"></span> <span class="text-zinc-500">Active</span></td>
      </tr>
      <tr class="hover:bg-zinc-50 dark:hover:bg-white/[.02]">
        <td class="px-4 py-3 text-zinc-900 dark:text-white">Jane Smith</td>
        <td class="px-4 py-3 text-zinc-500">jane@gatrion.com</td>
        <td class="px-4 py-3"><span class="rounded-md bg-zinc-500/10 px-2 py-0.5 text-xs font-medium text-zinc-600 dark:text-zinc-300">USER</span></td>
        <td class="px-4 py-3"><span class="inline-flex h-2 w-2 rounded-full bg-zinc-400"></span> <span class="text-zinc-500">Inactive</span></td>
      </tr>
    </tbody>
  </table>
</div>
</section>

<section class="animate-fade-up [animation-delay:.15s]">
<h2 id="tanstack-table" class="group scroll-mt-24 text-xl font-bold tracking-tight text-zinc-900 dark:text-white">
  <a href="#tanstack-table" class="flex items-center gap-2">TanStack Table Integration <span class="text-violet-400 opacity-0 transition group-hover:opacity-100">#</span></a>
</h2>

<CodeBlock code={`import { useReactTable, getCoreRowModel, getSortedRowModel, getPaginationRowModel, flexRender, ColumnDef } from '@tanstack/react-table';

const columns: ColumnDef<User>[] = [
  { accessorKey: 'name', header: 'Name' },
  { accessorKey: 'email', header: 'Email' },
  {
    accessorKey: 'role',
    header: 'Role',
    cell: ({ row }) => <Badge variant={row.original.role === 'ADMIN' ? 'primary' : 'default'}>{row.original.role}</Badge>,
  },
  {
    id: 'actions',
    cell: ({ row }) => (
      <DropdownMenu>
        <DropdownMenuTrigger>Edit</DropdownMenuTrigger>
        <DropdownMenuContent>
          <DropdownMenuItem onClick={() => handleEdit(row.original)}>Edit</DropdownMenuItem>
          <DropdownMenuItem onClick={() => handleDelete(row.original)}>Delete</DropdownMenuItem>
        </DropdownMenuContent>
      </DropdownMenu>
    ),
  },
];

function UsersTable({ data }: { data: User[] }) {
  const table = useReactTable({
    data,
    columns,
    getCoreRowModel: getCoreRowModel(),
    getSortedRowModel: getSortedRowModel(),
    getPaginationRowModel: getPaginationRowModel(),
  });

  return (
    <div>
      <table>
        <thead>
          {table.getHeaderGroups().map(hg => (
            <tr key={hg.id}>
              {hg.headers.map(header => (
                <th key={header.id} onClick={header.column.getToggleSortingHandler()}>
                  {flexRender(header.column.columnDef.header, header.getContext())}
                </th>
              ))}
            </tr>
          ))}
        </thead>
        <tbody>
          {table.getRowModel().rows.map(row => (
            <tr key={row.id}>
              {row.getVisibleCells().map(cell => (
                <td key={cell.id}>{flexRender(cell.column.columnDef.cell, cell.getContext())}</td>
              ))}
            </tr>
          ))}
        </tbody>
      </table>
      <Pagination table={table} />
    </div>
  );
}`} lang="bash" title="UsersTable.tsx" />
</section>

<section class="animate-fade-up [animation-delay:.2s]">
<h2 id="row-selection" class="group scroll-mt-24 text-xl font-bold tracking-tight text-zinc-900 dark:text-white">
  <a href="#row-selection" class="flex items-center gap-2">Row Selection <span class="text-violet-400 opacity-0 transition group-hover:opacity-100">#</span></a>
</h2>

<CodeBlock code={`const table = useReactTable({
  data,
  columns,
  enableRowSelection: true,
  onRowSelectionChange: setRowSelection,
  state: { rowSelection },
});

// checkbox di header
<th>
  <input
    type="checkbox"
    checked={table.getIsAllRowsSelected()}
    onChange={table.getToggleAllRowsSelectedHandler()}
  />
</th>

// bulk actions
{Object.keys(rowSelection).length > 0 && (
  <BulkActions
    selectedCount={Object.keys(rowSelection).length}
    onBulkDelete={() => handleBulkDelete(Object.keys(rowSelection))}
  />
)}`} lang="bash" title="row-selection.tsx" />
</section>

<section class="animate-fade-up [animation-delay:.25s]">
<h2 id="next-steps" class="group scroll-mt-24 text-xl font-bold tracking-tight text-zinc-900 dark:text-white">
  <a href="#next-steps" class="flex items-center gap-2">Selanjutnya <span class="text-violet-400 opacity-0 transition group-hover:opacity-100">#</span></a>
</h2>

<NextSteps>
  <NextStepCard title="Charts" description="Visualisasi data dengan charts." href="/docs/web-dashboard/charts/" />
  <NextStepCard title="Modals" description="Modal dan dialog components." href="/docs/web-dashboard/modals/" />
</NextSteps>
</section>
