---
title: "Performance Tips"
project: "Web Dashboard"
category: "Deployment"
description: "Tips dan best practices untuk mengoptimalkan performa Web Dashboard: bundle size, rendering, dan caching."
---

import Callout from '../../../components/Callout.astro';
import CodeBlock from '../../../components/CodeBlock.astro';
import NextSteps from '../../../components/NextSteps.astro';
import NextStepCard from '../../../components/NextStepCard.astro';

<section class="animate-fade-up [animation-delay:.1s]">
<h2 id="bundle-size" class="group scroll-mt-24 text-xl font-bold tracking-tight text-zinc-900 dark:text-white">
  <a href="#bundle-size" class="flex items-center gap-2">Reduce Bundle Size <span class="text-violet-400 opacity-0 transition group-hover:opacity-100">#</span></a>
</h2>

<CodeBlock code={`# analyze bundle
pnpm build --analyze

# atau dengan rollup-plugin-visualizer
pnpm add -D rollup-plugin-visualizer`} lang="bash" title="terminal" />

<Callout type="info">
  **Target:** Initial JS bundle &lt; 200KB gzipped. Gunakan code splitting untuk routes besar.
</Callout>

<CodeBlock code={`// vite.config.ts - manual chunks
export default defineConfig({
  build: {
    rollupOptions: {
      output: {
        manualChunks: {
          'react-vendor': ['react', 'react-dom', 'react-router-dom'],
          'query-vendor': ['@tanstack/react-query'],
          'chart-vendor': ['recharts'],
          'ui-vendor': ['@radix-ui/react-dialog', '@radix-ui/react-select'],
        },
      },
    },
  },
});`} lang="bash" title="vite.config.ts" />
</section>

<section class="animate-fade-up [animation-delay:.15s]">
<h2 id="rendering" class="group scroll-mt-24 text-xl font-bold tracking-tight text-zinc-900 dark:text-white">
  <a href="#rendering" class="flex items-center gap-2">Optimize Rendering <span class="text-violet-400 opacity-0 transition group-hover:opacity-100">#</span></a>
</h2>

<CodeBlock code={`// 1. useMemo untuk expensive calculations
const sortedData = useMemo(() => 
  data.sort((a, b) => a.name.localeCompare(b.name)),
  [data]
);

// 2. React.memo untuk component yang sering re-render
const UserRow = React.memo(({ user }: { user: User }) => (
  &lt;tr&gt;
    &lt;td&gt;\{user.name&lt;/td&gt;
    &lt;td&gt;\{user.email&lt;/td&gt;
  &lt;/tr&gt;,
  (prev, next) => prev.user.id === next.user.id
);

// 3. useCallback untuk event handlers
const handleDelete = useCallback((id: string) => {
  deleteUser.mutate(id);
}, [deleteUser]);

// 4. Virtualisasi untuk list besar
import { useVirtualizer } from '@tanstack/react-virtual';

function BigList({ items }: { items: Item[] }) {
  const parentRef = useRef(null);
  const virtualizer = useVirtualizer({
    count: items.length,
    getScrollElement: () => parentRef.current,
    estimateSize: () => 50,
  });

  return (
    &lt;div ref={parentRef} style={{ height: 600, overflow: 'auto' }}&gt;
      &lt;div style={{ height: virtualizer.getTotalSize() }}&gt;
        {virtualizer.getVirtualItems().map(virtualItem =&gt; (
          &lt;div key={virtualItem.key} style={{ 
            position: 'absolute', 
            top: virtualItem.start, 
            height: virtualItem.size 
          }}&gt;
            {items[virtualItem.index].name}
          &lt;/div&gt;
        ))}
      &lt;/div&gt;
    &lt;/div&gt;
  );
}`} lang="bash" title="rendering-optimization.tsx" />
</section>

<section class="animate-fade-up [animation-delay:.2s]">
<h2 id="caching" class="group scroll-mt-24 text-xl font-bold tracking-tight text-zinc-900 dark:text-white">
  <a href="#caching" class="flex items-center gap-2">Data Caching <span class="text-violet-400 opacity-0 transition group-hover:opacity-100">#</span></a>
</h2>

<CodeBlock code={`// staleTime: data dianggap fresh selama 5 menit
export function useUsers() {
  return useQuery({
    queryKey: ['users'],
    queryFn: () => apiClient.get('/users').then(res => res.data),
    staleTime: 5 * 60 * 1000,    // 5 menit
    cacheTime: 30 * 60 * 1000,   // 30 menit di cache
    refetchOnWindowFocus: false,  // tidak refetch saat focus
  });
}

// Prefetch pada hover
function UserLink({ userId }: { userId: string }) {
  const queryClient = useQueryClient();
  
  return (
    &lt;Link
      to={`/users/${userId}`
      onMouseEnter={() => {
        queryClient.prefetchQuery({
          queryKey: ['user', userId],
          queryFn: () => apiClient.get(`/users/${userId}`).then(res => res.data),
          staleTime: 60 * 1000,
        });
      }}
    &gt;
      View Profile
    &lt;/Link&gt;
  );
}`} lang="bash" title="caching.ts" />
</section>

<section class="animate-fade-up [animation-delay:.25s]">
<h2 id="images" class="group scroll-mt-24 text-xl font-bold tracking-tight text-zinc-900 dark:text-white">
  <a href="#images" class="flex items-center gap-2">Image Optimization <span class="text-violet-400 opacity-0 transition group-hover:opacity-100">#</span></a>
</h2>

<CodeBlock code={`// lazy load images
&lt;img 
  src={image.url} 
  loading="lazy" 
  decoding="async"
  alt={image.alt}
/&gt;

// responsive images
&lt;picture&gt;
  &lt;source srcset="/img/hero-mobile.webp" media="(max-width: 640px)" type="image/webp" /&gt;
  &lt;source srcset="/img/hero-desktop.webp" media="(min-width: 641px)" type="image/webp" /&gt;
  &lt;img src="/img/hero-fallback.jpg" alt="Hero" loading="lazy" /&gt;
&lt;/picture&gt;`} lang="bash" title="image-optimization.tsx" />
</section>

<section class="animate-fade-up [animation-delay:.3s]">
<h2 id="lighthouse" class="group scroll-mt-24 text-xl font-bold tracking-tight text-zinc-900 dark:text-white">
  <a href="#lighthouse" class="flex items-center gap-2">Lighthouse Score Target <span class="text-violet-400 opacity-0 transition group-hover:opacity-100">#</span></a>
</h2>

<div class="mt-4 overflow-hidden rounded-2xl border border-zinc-200 bg-white shadow-sm dark:border-white/[.08] dark:bg-[#0d0d10]">
  <pre class="overflow-x-auto p-5 text-[13px] leading-7"><code class="font-mono text-zinc-800 dark:text-zinc-200"><span class="text-emerald-500">━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━</span>
<span class="text-emerald-500">Performance</span>       <span class="text-emerald-500">  95+</span>
<span class="text-emerald-500">Accessibility</span>     <span class="text-emerald-500">  100</span>
<span class="text-emerald-500">Best Practices</span>    <span class="text-emerald-500">  100</span>
<span class="text-emerald-500">SEO</span>               <span class="text-emerald-500">  95+</span>
<span class="text-zinc-500">───────────────────────────────</span>
<span class="text-zinc-500"># Run audit:</span>
<span class="text-cyan-500">npx lighthouse</span> <span class="text-zinc-800 dark:text-zinc-200">https://dashboard.gatrion.com --view</span></code></pre>
</div>
</section>

<section class="animate-fade-up [animation-delay:.35s]">
<h2 id="next-steps" class="group scroll-mt-24 text-xl font-bold tracking-tight text-zinc-900 dark:text-white">
  <a href="#next-steps" class="flex items-center gap-2">Selanjutnya <span class="text-violet-400 opacity-0 transition group-hover:opacity-100">#</span></a>
</h2>

<NextSteps>
  <NextStepCard title="Overview" description="Kembali ke halaman overview." href="/docs/web-dashboard/overview/" />
  <NextStepCard title="Installation" description="Setup dari awal." href="/docs/web-dashboard/installation/" />
</NextSteps>
</section>
