---
title: "Charts"
project: "Web Dashboard"
category: "Components"
description: "Visualisasi data dengan Recharts: Line, Bar, Area, dan Pie charts untuk analytics dashboard."
---

import Callout from '../../../components/Callout.astro';
import CodeBlock from '../../../components/CodeBlock.astro';
import NextSteps from '../../../components/NextSteps.astro';
import NextStepCard from '../../../components/NextStepCard.astro';

<section class="animate-fade-up [animation-delay:.1s]">
<h2 id="line-chart" class="group scroll-mt-24 text-xl font-bold tracking-tight text-zinc-900 dark:text-white">
  <a href="#line-chart" class="flex items-center gap-2">Line Chart <span class="text-violet-400 opacity-0 transition group-hover:opacity-100">#</span></a>
</h2>

<CodeBlock code={`import { LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer } from 'recharts';

const data = [
  { month: 'Jan', revenue: 4500000, orders: 120 },
  { month: 'Feb', revenue: 5200000, orders: 145 },
  { month: 'Mar', revenue: 4800000, orders: 132 },
  { month: 'Apr', revenue: 6100000, orders: 178 },
];

<ResponsiveContainer width="100%" height={300}>
  <LineChart data={data}>
    <CartesianGrid strokeDasharray="3 3" stroke="#e4e4e7" />
    <XAxis dataKey="month" stroke="#71717a" fontSize={12} />
    <YAxis stroke="#71717a" fontSize={12} tickFormatter={(v) => \`\${v/1000000}M\`} />
    <Tooltip formatter={(v) => \`Rp \${v.toLocaleString('id-ID')}\`} />
    <Line type="monotone" dataKey="revenue" stroke="#8b5cf6" strokeWidth={2} dot={{ r: 4 }} />
  </LineChart>
</ResponsiveContainer>`} lang="bash" title="LineChart.tsx" />
</section>

<section class="animate-fade-up [animation-delay:.15s]">
<h2 id="bar-chart" class="group scroll-mt-24 text-xl font-bold tracking-tight text-zinc-900 dark:text-white">
  <a href="#bar-chart" class="flex items-center gap-2">Bar Chart <span class="text-violet-400 opacity-0 transition group-hover:opacity-100">#</span></a>
</h2>

<CodeBlock code={`import { BarChart, Bar, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer } from 'recharts';

<ResponsiveContainer width="100%" height={300}>
  <BarChart data={data}>
    <CartesianGrid strokeDasharray="3 3" stroke="#e4e4e7" />
    <XAxis dataKey="month" stroke="#71717a" fontSize={12} />
    <YAxis stroke="#71717a" fontSize={12} />
    <Tooltip />
    <Bar dataKey="orders" fill="#8b5cf6" radius={[8, 8, 0, 0]} />
  </BarChart>
</ResponsiveContainer>`} lang="bash" title="BarChart.tsx" />
</section>

<section class="animate-fade-up [animation-delay:.2s]">
<h2 id="area-chart" class="group scroll-mt-24 text-xl font-bold tracking-tight text-zinc-900 dark:text-white">
  <a href="#area-chart" class="flex items-center gap-2">Area Chart <span class="text-violet-400 opacity-0 transition group-hover:opacity-100">#</span></a>
</h2>

<CodeBlock code={`import { AreaChart, Area, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer } from 'recharts';

<ResponsiveContainer width="100%" height={300}>
  <AreaChart data={data}>
    <defs>
      <linearGradient id="colorRev" x1="0" y1="0" x2="0" y2="1">
        <stop offset="5%" stopColor="#8b5cf6" stopOpacity={0.3} />
        <stop offset="95%" stopColor="#8b5cf6" stopOpacity={0} />
      </linearGradient>
    </defs>
    <CartesianGrid strokeDasharray="3 3" stroke="#e4e4e7" />
    <XAxis dataKey="month" stroke="#71717a" fontSize={12} />
    <YAxis stroke="#71717a" fontSize={12} />
    <Tooltip />
    <Area type="monotone" dataKey="revenue" stroke="#8b5cf6" fill="url(#colorRev)" strokeWidth={2} />
  </AreaChart>
</ResponsiveContainer>`} lang="bash" title="AreaChart.tsx" />
</section>

<section class="animate-fade-up [animation-delay:.25s]">
<h2 id="pie-chart" class="group scroll-mt-24 text-xl font-bold tracking-tight text-zinc-900 dark:text-white">
  <a href="#pie-chart" class="flex items-center gap-2">Pie Chart <span class="text-violet-400 opacity-0 transition group-hover:opacity-100">#</span></a>
</h2>

<CodeBlock code={`import { PieChart, Pie, Cell, Tooltip, Legend, ResponsiveContainer } from 'recharts';

const pieData = [
  { name: 'BCA VA', value: 45, color: '#8b5cf6' },
  { name: 'QRIS', value: 30, color: '#06b6d4' },
  { name: 'GoPay', value: 15, color: '#ec4899' },
  { name: 'Manual', value: 10, color: '#f59e0b' },
];

<ResponsiveContainer width="100%" height={300}>
  <PieChart>
    <Pie data={pieData} dataKey="value" nameKey="name" cx="50%" cy="50%" outerRadius={100}>
      {pieData.map((entry, i) => <Cell key={i} fill={entry.color} />)}
    </Pie>
    <Tooltip />
    <Legend />
  </PieChart>
</ResponsiveContainer>`} lang="bash" title="PieChart.tsx" />
</section>

<section class="animate-fade-up [animation-delay:.3s]">
<h2 id="next-steps" class="group scroll-mt-24 text-xl font-bold tracking-tight text-zinc-900 dark:text-white">
  <a href="#next-steps" class="flex items-center gap-2">Selanjutnya <span class="text-violet-400 opacity-0 transition group-hover:opacity-100">#</span></a>
</h2>

<NextSteps>
  <NextStepCard title="Modals" description="Modal dan dialog components." href="/docs/web-dashboard/modals/" />
  <NextStepCard title="Authentication" description="Auth flow implementation." href="/docs/web-dashboard/authentication/" />
</NextSteps>
</section>
