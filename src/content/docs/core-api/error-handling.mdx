---
title: "Error Handling"
project: "Core API"
category: "Panduan"
description: "Standardisasi error response di Core API: exception filters, custom exceptions, dan error codes."
---

import Callout from '../../../components/Callout.astro';
import CodeBlock from '../../../components/CodeBlock.astro';
import NextSteps from '../../../components/NextSteps.astro';
import NextStepCard from '../../../components/NextStepCard.astro';

<section class="animate-fade-up [animation-delay:.1s]">
  <h2 id="standard-response" class="group scroll-mt-24 text-xl font-bold tracking-tight text-zinc-900 dark:text-white">
    <a href="#standard-response" class="flex items-center gap-2">Standard Error Response <span class="text-violet-400 opacity-0 transition group-hover:opacity-100">#</span></a>
  </h2>

  <p class="mt-3 leading-relaxed">Semua error mengikuti format response yang konsisten:</p>

  <CodeBlock code={`{
  "statusCode": 400,
  "error": "Bad Request",
  "message": "Email already exists",
  "code": "USER_EMAIL_EXISTS",
  "timestamp": "2025-01-30T12:00:00.000Z",
  "path": "/api/users"
}`} lang="json" title="Error Response Format" />
</section>

<section class="animate-fade-up [animation-delay:.15s]">
  <h2 id="exception-filter" class="group scroll-mt-24 text-xl font-bold tracking-tight text-zinc-900 dark:text-white">
    <a href="#exception-filter" class="flex items-center gap-2">Global Exception Filter <span class="text-violet-400 opacity-0 transition group-hover:opacity-100">#</span></a>
  </h2>

  <CodeBlock code={`// src/common/filters/all-exceptions.filter.ts
@Catch()
export class AllExceptionsFilter implements ExceptionFilter {
  catch(exception: unknown, host: ArgumentsHost) {
    const ctx = host.switchToHttp();
    const response = ctx.getResponse();
    const request = ctx.getRequest();

    let status = 500;
    let message = 'Internal Server Error';
    let code = 'INTERNAL_ERROR';

    if (exception instanceof HttpException) {
      status = exception.getStatus();
      const res = exception.getResponse();
      message = typeof res === 'string' ? res : (res as any).message;
      code = (res as any).code || exception.name;
    } else if (exception instanceof Prisma.PrismaClientKnownRequestError) {
      const mapped = this.mapPrismaError(exception);
      status = mapped.status;
      message = mapped.message;
      code = mapped.code;
    }

    response.status(status).json({
      statusCode: status,
      error: message,
      code,
      timestamp: new Date().toISOString(),
      path: request.url,
    });
  }
}`} lang="typescript" title="all-exceptions.filter.ts" />
</section>

<section class="animate-fade-up [animation-delay:.2s]">
  <h2 id="custom-exceptions" class="group scroll-mt-24 text-xl font-bold tracking-tight text-zinc-900 dark:text-white">
    <a href="#custom-exceptions" class="flex items-center gap-2">Custom Exceptions <span class="text-violet-400 opacity-0 transition group-hover:opacity-100">#</span></a>
  </h2>

  <CodeBlock code={`// src/common/exceptions/business.exception.ts
export class BusinessException extends HttpException {
  constructor(message: string, code: string, status = 400) {
    super({ message, code }, status);
  }
}

// usage
throw new BusinessException(
  'Insufficient balance',
  'WALLET_INSUFFICIENT_BALANCE',
  422
);`} lang="typescript" title="business.exception.ts" />
</section>

<section class="animate-fade-up [animation-delay:.25s]">
  <h2 id="error-codes" class="group scroll-mt-24 text-xl font-bold tracking-tight text-zinc-900 dark:text-white">
    <a href="#error-codes" class="flex items-center gap-2">Error Codes <span class="text-violet-400 opacity-0 transition group-hover:opacity-100">#</span></a>
  </h2>

  <p class="mt-3 leading-relaxed">Daftar error codes yang umum digunakan:</p>

  <div class="mt-4 overflow-hidden rounded-2xl border border-zinc-200 bg-white shadow-sm dark:border-white/[.08] dark:bg-[#0d0d10]">
  <pre class="overflow-x-auto p-5 text-[13px] leading-7"><code class="font-mono text-zinc-800 dark:text-zinc-200"># Auth
AUTH_INVALID_CREDENTIALS     # 401
AUTH_TOKEN_EXPIRED            # 401
AUTH_TOKEN_INVALID            # 401
AUTH_FORBIDDEN                # 403

# Users
USER_NOT_FOUND                # 404
USER_EMAIL_EXISTS             # 409

# Orders
ORDER_NOT_FOUND               # 404
ORDER_INVALID_STATUS          # 422

# Payment
PAYMENT_FAILED                # 402
WALLET_INSUFFICIENT_BALANCE   # 422</code></pre>
</div>
</section>

<section class="animate-fade-up [animation-delay:.3s]">
  <h2 id="prisma-errors" class="group scroll-mt-24 text-xl font-bold tracking-tight text-zinc-900 dark:text-white">
    <a href="#prisma-errors" class="flex items-center gap-2">Mapping Prisma Errors <span class="text-violet-400 opacity-0 transition group-hover:opacity-100">#</span></a>
  </h2>

  <CodeBlock code={`private mapPrismaError(error: PrismaClientKnownRequestError) {
  switch (error.code) {
    case 'P2002': // Unique constraint
      return { status: 409, message: 'Resource already exists', code: 'DUPLICATE_ENTRY' };
    case 'P2025': // Record not found
      return { status: 404, message: 'Resource not found', code: 'NOT_FOUND' };
    case 'P2003': // Foreign key constraint
      return { status: 400, message: 'Referenced resource not found', code: 'FOREIGN_KEY_VIOLATION' };
    default:
      return { status: 500, message: 'Database error', code: 'DATABASE_ERROR' };
  }
}`} lang="typescript" title="prisma-error-mapping.ts" />
</section>

<section class="animate-fade-up [animation-delay:.35s]">
  <h2 id="next-steps" class="group scroll-mt-24 text-xl font-bold tracking-tight text-zinc-900 dark:text-white">
    <a href="#next-steps" class="flex items-center gap-2">Selanjutnya <span class="text-violet-400 opacity-0 transition group-hover:opacity-100">#</span></a>
  </h2>

  <NextSteps>
    <NextStepCard 
      title="Testing" 
      description="Unit dan e2e testing dengan Jest."
      href="/docs/core-api/testing/" />
    <NextStepCard 
      title="Deployment" 
      description="Deploy Core API ke production dengan Docker."
      href="/docs/core-api/deployment/" />
  </NextSteps>
</section>
