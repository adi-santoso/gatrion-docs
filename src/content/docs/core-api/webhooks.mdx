---
title: "Webhooks"
project: "Core API"
category: "API Reference"
description: "Webhook events untuk payment notifications, order updates, dan real-time integrations."
---

import Callout from '../../../components/Callout.astro';
import CodeBlock from '../../../components/CodeBlock.astro';
import NextSteps from '../../../components/NextSteps.astro';
import NextStepCard from '../../../components/NextStepCard.astro';

<section class="animate-fade-up [animation-delay:.1s]">
<h2 id="overview" class="group scroll-mt-24 text-xl font-bold tracking-tight text-zinc-900 dark:text-white">
  <a href="#overview" class="flex items-center gap-2">Overview <span class="text-violet-400 opacity-0 transition group-hover:opacity-100">#</span></a>
</h2>

<p class="mt-3 leading-relaxed">Webhooks mengirim HTTP POST notification ke endpoint yang sudah terdaftar setiap kali event terjadi. Daftarkan webhook URL via dashboard atau API.</p>
</section>

<section class="animate-fade-up [animation-delay:.15s]">
<h2 id="events" class="group scroll-mt-24 text-xl font-bold tracking-tight text-zinc-900 dark:text-white">
  <a href="#events" class="flex items-center gap-2">Available Events <span class="text-violet-400 opacity-0 transition group-hover:opacity-100">#</span></a>
</h2>

<div class="mt-4 overflow-hidden rounded-2xl border border-zinc-200 bg-white shadow-sm dark:border-white/[.08] dark:bg-[#0d0d10]">
  <pre class="overflow-x-auto p-5 text-[13px] leading-7"><code class="font-mono text-zinc-800 dark:text-zinc-200"><span class="text-violet-500 dark:text-violet-400">payment.created</span>        <span class="text-zinc-500"># Payment dibuat</span>
<span class="text-violet-500 dark:text-violet-400">payment.settled</span>         <span class="text-zinc-500"># Payment berhasil</span>
<span class="text-violet-500 dark:text-violet-400">payment.failed</span>           <span class="text-zinc-500"># Payment gagal</span>
<span class="text-violet-500 dark:text-violet-400">payment.refunded</span>        <span class="text-zinc-500"># Payment di-refund</span>
<span class="text-violet-500 dark:text-violet-400">order.created</span>            <span class="text-zinc-500"># Order dibuat</span>
<span class="text-violet-500 dark:text-violet-400">order.status_changed</span>    <span class="text-zinc-500"># Status order berubah</span>
<span class="text-violet-500 dark:text-violet-400">user.registered</span>         <span class="text-zinc-500"># User baru mendaftar</span>
<span class="text-violet-500 dark:text-violet-400">user.updated</span>             <span class="text-zinc-500"># Profile user di-update</span></code></pre>
</div>
</section>

<section class="animate-fade-up [animation-delay:.2s]">
<h2 id="webhook-payload" class="group scroll-mt-24 text-xl font-bold tracking-tight text-zinc-900 dark:text-white">
  <a href="#webhook-payload" class="flex items-center gap-2">Webhook Payload <span class="text-violet-400 opacity-0 transition group-hover:opacity-100">#</span></a>
</h2>

<CodeBlock code={`POST <your-webhook-url>
Content-Type: application/json
X-Gatrion-Signature: sha256=abc123...

{
  "event": "payment.settled",
  "timestamp": "2025-01-30T12:30:00.000Z",
  "data": {
    "id": "pay_789",
    "orderId": "order_456",
    "amount": "225000.00",
    "method": "MIDTRANS",
    "status": "SETTLED",
    "paidAt": "2025-01-30T12:30:00.000Z"
  }
}`} lang="bash" title="Webhook Payload" />
</section>

<section class="animate-fade-up [animation-delay:.25s]">
<h2 id="signature-verification" class="group scroll-mt-24 text-xl font-bold tracking-tight text-zinc-900 dark:text-white">
  <a href="#signature-verification" class="flex items-center gap-2">Signature Verification <span class="text-violet-400 opacity-0 transition group-hover:opacity-100">#</span></a>
</h2>

<p class="mt-3 leading-relaxed">Verifikasi signature untuk memastikan webhook berasal dari Gatrion:</p>

<CodeBlock code={`import crypto from 'crypto';

function verifyWebhookSignature(
  payload: string,
  signature: string,
  secret: string
): boolean {
  const expected = 'sha256=' + 
    crypto.createHmac('sha256', secret).update(payload).digest('hex');
  return crypto.timingSafeEqual(
    Buffer.from(expected),
    Buffer.from(signature)
  );
}

// Express middleware
app.post('/webhook', (req, res) => {
  const signature = req.headers['x-gatrion-signature'] as string;
  const rawBody = JSON.stringify(req.body);

  if (!verifyWebhookSignature(rawBody, signature, process.env.WEBHOOK_SECRET)) {
    return res.status(401).json({ error: 'Invalid signature' });
  }

  // Process webhook
  const { event, data } = req.body;
  console.log(\`Received: \${event}\`, data);

  res.json({ received: true });
});`} lang="bash" title="signature-verification.ts" />
</section>

<section class="animate-fade-up [animation-delay:.3s]">
<h2 id="retry" class="group scroll-mt-24 text-xl font-bold tracking-tight text-zinc-900 dark:text-white">
  <a href="#retry" class="flex items-center gap-2">Retry Policy <span class="text-violet-400 opacity-0 transition group-hover:opacity-100">#</span></a>
</h2>

<Callout type="info">
  **Retry:** Jika endpoint mengembalikan non-2xx, sistem akan retry hingga 5x dengan exponential backoff: 1s, 2s, 4s, 8s, 16s.
</Callout>

<p class="mt-4 leading-relaxed">Endpoint harus merespons dengan status <code class="rounded-md bg-zinc-100 px-1.5 py-0.5 font-mono text-[13px] dark:bg-white/[.06]">200</code> dalam waktu <strong>10 detik</strong>. Jika timeout, akan dianggap gagal dan di-retry.</p>
</section>

<section class="animate-fade-up [animation-delay:.35s]">
<h2 id="next-steps" class="group scroll-mt-24 text-xl font-bold tracking-tight text-zinc-900 dark:text-white">
  <a href="#next-steps" class="flex items-center gap-2">Selanjutnya <span class="text-violet-400 opacity-0 transition group-hover:opacity-100">#</span></a>
</h2>

<NextSteps>
  <NextStepCard 
    title="Changelog" 
    description="Riwayat perubahan dan update terbaru."
    href="/docs/core-api/changelog/" />
  <NextStepCard 
    title="FAQ" 
    description="Pertanyaan yang sering diajukan."
    href="/docs/core-api/faq/" />
</NextSteps>
</section>
