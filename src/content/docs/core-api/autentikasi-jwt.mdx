---
title: "Autentikasi & JWT"
project: "Core API"
category: "Panduan"
description: "Implementasi autentikasi JWT di Core API: login, refresh token, guards, dan role-based access control (RBAC)."
---

import Callout from '../../../components/Callout.astro';
import CodeBlock from '../../../components/CodeBlock.astro';
import NextSteps from '../../../components/NextSteps.astro';
import NextStepCard from '../../../components/NextStepCard.astro';

<section class="animate-fade-up [animation-delay:.1s]">
<h2 id="overview" class="group scroll-mt-24 text-xl font-bold tracking-tight text-zinc-900 dark:text-white">
  <a href="#overview" class="flex items-center gap-2">Overview <span class="text-violet-400 opacity-0 transition group-hover:opacity-100">#</span></a>
</h2>

<p class="mt-3 leading-relaxed">Core API menggunakan <strong>JWT (JSON Web Token)</strong> untuk autentikasi. Token memiliki masa berlaku dan dapat di-refresh menggunakan refresh token.</p>

<Callout type="info">
  **Flow:** Login → Access Token (1d) + Refresh Token (7d) → Refresh → New Access Token
</Callout>
</section>

<section class="animate-fade-up [animation-delay:.15s]">
<h2 id="login-endpoint" class="group scroll-mt-24 text-xl font-bold tracking-tight text-zinc-900 dark:text-white">
  <a href="#login-endpoint" class="flex items-center gap-2">Login Endpoint <span class="text-violet-400 opacity-0 transition group-hover:opacity-100">#</span></a>
</h2>

<div class="mt-3 flex items-center gap-2">
  <code class="font-mono text-[13px] text-zinc-600 dark:text-zinc-300">POST /api/auth/login</code>
</div>

<CodeBlock code={`{
  "email": "user@example.com",
  "password": "secret123"
}`} lang="bash" title="Request Body" />

<CodeBlock code={`{
  "statusCode": 200,
  "data": {
    "accessToken": "eyJhbGciOiJIUzI1NiIs...",
    "refreshToken": "eyJhbGciOiJIUzI1NiIs...",
    "user": {
      "id": "clxyz123",
      "email": "user@example.com",
      "name": "John Doe",
      "role": "ADMIN"
    }
  }
}`} lang="json" title="Response" />
</section>

<section class="animate-fade-up [animation-delay:.2s]">
<h2 id="implementation" class="group scroll-mt-24 text-xl font-bold tracking-tight text-zinc-900 dark:text-white">
  <a href="#implementation" class="flex items-center gap-2">Implementation <span class="text-violet-400 opacity-0 transition group-hover:opacity-100">#</span></a>
</h2>

<CodeBlock code={`// auth.service.ts
import { JwtService } from '@nestjs/jwt';
import * as bcrypt from 'bcrypt';

@Injectable()
export class AuthService {
  constructor(private jwtService: JwtService) {}

  async login(email: string, password: string) {
    const user = await this.usersService.findByEmail(email);
    if (!user) throw new UnauthorizedException();

    const valid = await bcrypt.compare(password, user.password);
    if (!valid) throw new UnauthorizedException();

    const payload = { sub: user.id, email: user.email, role: user.role };
    
    return {
      accessToken: this.jwtService.sign(payload, { expiresIn: '1d' }),
      refreshToken: this.jwtService.sign(payload, { expiresIn: '7d' }),
      user: { id: user.id, email: user.email, name: user.name, role: user.role },
    };
  }
}`} lang="typescript" title="auth.service.ts" />
</section>

<section class="animate-fade-up [animation-delay:.25s]">
<h2 id="jwt-guard" class="group scroll-mt-24 text-xl font-bold tracking-tight text-zinc-900 dark:text-white">
  <a href="#jwt-guard" class="flex items-center gap-2">JWT Guard <span class="text-violet-400 opacity-0 transition group-hover:opacity-100">#</span></a>
</h2>

<p class="mt-3 leading-relaxed">Guard untuk memprotect endpoint dengan JWT verification:</p>

<CodeBlock code={`// jwt-auth.guard.ts
@Injectable()
export class JwtAuthGuard implements CanActivate {
  constructor(private jwtService: JwtService) {}

  async canActivate(context: ExecutionContext) {
    const request = context.switchToHttp().getRequest();
    const authHeader = request.headers.authorization;
    
    if (!authHeader?.startsWith('Bearer ')) {
      throw new UnauthorizedException('Missing token');
    }

    const token = authHeader.split(' ')[1];
    try {
      const payload = this.jwtService.verify(token);
      request.user = payload;
      return true;
    } catch {
      throw new UnauthorizedException('Invalid token');
    }
  }
}`} lang="typescript" title="jwt-auth.guard.ts" />
</section>

<section class="animate-fade-up [animation-delay:.3s]">
<h2 id="rbac" class="group scroll-mt-24 text-xl font-bold tracking-tight text-zinc-900 dark:text-white">
  <a href="#rbac" class="flex items-center gap-2">Role-Based Access Control <span class="text-violet-400 opacity-0 transition group-hover:opacity-100">#</span></a>
</h2>

<p class="mt-3 leading-relaxed">Dengan decorator <code class="rounded-md bg-zinc-100 px-1.5 py-0.5 font-mono text-[13px] dark:bg-white/[.06]">@Roles()</code> dan <code class="rounded-md bg-zinc-100 px-1.5 py-0.5 font-mono text-[13px] dark:bg-white/[.06]">RolesGuard</code>:</p>

<CodeBlock code={`// roles.decorator.ts
export const ROLES_KEY = 'roles';
export const Roles = (...roles: string[]) => 
  (target: any, key?: string, descriptor?: PropertyDescriptor) => {
  if (descriptor) {
    Reflect.defineMetadata(ROLES_KEY, roles, descriptor.value);
  }
};

// usage di controller
@Get('admin')
@UseGuards(JwtAuthGuard, RolesGuard)
@Roles('ADMIN')
findAllAdmin(@Req() req) {
  return this.usersService.findAllByAdmin(req.user.id);
}`} lang="typescript" title="roles.decorator.ts" />
</section>

<section class="animate-fade-up [animation-delay:.35s]">
<h2 id="refresh-token" class="group scroll-mt-24 text-xl font-bold tracking-tight text-zinc-900 dark:text-white">
  <a href="#refresh-token" class="flex items-center gap-2">Refresh Token <span class="text-violet-400 opacity-0 transition group-hover:opacity-100">#</span></a>
</h2>

<div class="mt-3 flex items-center gap-2">
  <code class="font-mono text-[13px] text-zinc-600 dark:text-zinc-300">POST /api/auth/refresh</code>
</div>

<CodeBlock code={`{
  "refreshToken": "eyJhbGciOiJIUzI1NiIs..."
}`} lang="bash" title="Request Body" />

<p class="mt-4 leading-relaxed">Response akan mengembalikan access token baru:</p>

<CodeBlock code={`{
  "statusCode": 200,
  "data": {
    "accessToken": "eyJhbGciOiJIUzI1NiIs...new"
  }
}`} lang="json" title="Response" />
</section>

<section class="animate-fade-up [animation-delay:.4s]">
<h2 id="next-steps" class="group scroll-mt-24 text-xl font-bold tracking-tight text-zinc-900 dark:text-white">
  <a href="#next-steps" class="flex items-center gap-2">Selanjutnya <span class="text-violet-400 opacity-0 transition group-hover:opacity-100">#</span></a>
</h2>

<NextSteps>
  <NextStepCard 
    title="Database & Migrasi" 
    description="Prisma schema dan migrasi database."
    href="/docs/core-api/database-migrasi/" />
  <NextStepCard 
    title="Error Handling" 
    description="Standardisasi error response dan exception filters."
    href="/docs/core-api/error-handling/" />
</NextSteps>
</section>
