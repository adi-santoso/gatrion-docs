---
/**
 * File tree container. Pair with `<File>` children.
 *
 *   <FileTree>
 *     <File name="src" type="folder" depth={0} />
 *     <File name="index.ts" depth={1} description="Entry point" />
 *   </FileTree>
 */
---

<div class="not-prose my-6 rounded-2xl border border-zinc-200 bg-white/70 p-4 font-mono text-[13px] dark:border-white/[0.08] dark:bg-white/[0.025]">
  <slot />
</div>
